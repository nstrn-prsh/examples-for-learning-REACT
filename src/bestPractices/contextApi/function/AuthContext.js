import React, { createContext } from "react";

/*in meghdare default baraye zamani ke provider nadarim
ya darim test nevisis mikonim be kar miad*/
const AuthContext = createContext({ id: 1 });

export default AuthContext;
