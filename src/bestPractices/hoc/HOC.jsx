import React, { Component } from "react";
import Input from "./Input";

export default class HOC extends Component {
     render() {
          return (
               <>
                    <Input />
                    <Input />
                    <Input />
               </>
          );
     }
}
