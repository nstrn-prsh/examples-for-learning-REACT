import Main from "./Main";
import { BrowserRouter } from "react-router-dom";

const Root = () => {
     return (
          <BrowserRouter>
               <Main />
          </BrowserRouter>
     );
};

export default Root;
